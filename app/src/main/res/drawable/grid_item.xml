<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- Change to the color of your choice -->
    <stroke
        android:width="2dp"
    android:color="@android:color/black" />
</shape>